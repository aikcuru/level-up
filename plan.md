# План разработки Level Up

## Цель документа

Этот документ разбивает разработку MVP Level Up на последовательные завершённые шаги. Каждый шаг должен давать работающий и проверяемый результат, соответствующий `concept.md` и `tz.md`.

План не расширяет согласованный MVP. Для реализации используются только:

- `index.html`;
- `style.css`;
- `script.js`;
- HTML;
- CSS;
- JavaScript;
- `localStorage`.

React, другие фреймворки, сервер, база данных, сборщики и дополнительные библиотеки не используются.

## Правила ведения plan.md как журнала разработки

1. Перед началом шага изменить его статус с `Не начат` на `В работе`.
2. Не начинать следующий шаг, пока результат текущего шага не реализован и не проверен.
3. После реализации записать под шагом фактически выполненные автоматические проверки и их результаты.
4. Автоматической считается только проверка, которую Codex действительно выполнил доступным инструментом и для которой получил наблюдаемый результат.
5. Нельзя отмечать проверку пройденной только потому, что код выглядит правильным при чтении.
6. Если в текущей среде недоступны запуск браузера, проверка его консоли, изменение ширины экрана или автоматизация пользовательских кликов, прямо записать, что проверка не выполнена автоматически, перенести её в список ручных проверок пользователя и не считать выполненной.
7. Для каждой выполненной автоматической проверки записывать использованную команду или инструмент и фактический результат, включая обнаруженные ошибки или подтверждение их отсутствия.
8. После тестов, изменяющих `localStorage`, обязательно восстановить чистое согласованное состояние приложения и зафиксировать результат восстановления.
9. Шаг нельзя переводить в статус `Завершён`, пока пользователь не подтвердил обязательные ручные проверки.
10. Если проверка не пройдена, оставить шаг в статусе `В работе` или установить `Заблокирован`, кратко указав причину.
11. После успешных автоматических проверок установить статус `На ручной проверке` и передать пользователю перечисленные ручные проверки, включая проверки, которые не удалось автоматизировать в текущей среде.
12. Статус `Завершён` устанавливать только после выполнения всех критериев завершения шага и подтверждения обязательных ручных проверок пользователем.
13. Git-точку сохранения создавать только после успешного завершения шага. Один шаг — один отдельный осмысленный commit.
14. Не добавлять в журнал новые функции, отсутствующие в `concept.md` и `tz.md`. Новые решения сначала согласовывать с пользователем.
15. При изменении требований фиксировать принятое решение в соответствующем шаге и повторно проверять затронутые завершённые шаги.

## Обозначения статусов

- `Не начат` — работа по шагу ещё не выполнялась.
- `В работе` — реализация или исправление проверок выполняется.
- `На ручной проверке` — автоматические проверки пройдены, требуется проверка пользователя.
- `Завершён` — результат реализован, проверен и может служить основой следующего шага.
- `Заблокирован` — продолжение невозможно без решения пользователя или устранения указанной причины.

---

## Шаг 1. Каркас приложения, первый запуск и базовое состояние

**Статус:** Завершён

### Цель шага

Получить открывающуюся адаптивную страницу с базовым пастельным оформлением, обязательным вводом имени при первом запуске, профилем начального уровня и безопасным хранением начального состояния.

### Файлы

- создать `index.html`;
- создать `style.css`;
- создать `script.js`.

### Что реализовать

- семантический каркас всех основных блоков страницы без полной функциональности последующих шагов;
- стартовую форму имени, которая блокирует основной интерфейс до ввода непустого имени;
- удаление внешних пробелов и текстовую ошибку для пустого имени;
- объект состояния версии 1 с `profile`, `additionalSubjects` и `tasks`;
- начальные значения: уровень 1, 0 XP, прогресс 0 из 100 и 100 XP до следующего уровня;
- сохранение всего постоянного состояния под единственным ключом `levelUpState`;
- безопасное чтение JSON при загрузке;
- переход к безопасному начальному состоянию и повторный запрос имени при отсутствии данных, невалидном JSON или критически некорректной структуре;
- базовую адаптивную компоновку без горизонтальной прокрутки;
- основу согласованной кремово-бежевой и коричневой цветовой системы.

### Результат шага

Приложение открывается напрямую через `index.html`. Новый пользователь вводит имя и видит основной экран с профилем уровня 1 и 0 XP. После перезагрузки имя и начальное состояние восстанавливаются.

### Автоматические проверки Codex

Все проверки ниже выполняются автоматически только при наличии подходящего доступного инструмента. Для каждой выполненной проверки в журнал записываются инструмент или команда и фактический результат. Проверки, которые нельзя выполнить инструментально, переносятся в ручные проверки пользователя и не считаются пройденными автоматически.

- проверить синтаксис `script.js`;
- при наличии управления браузером проверить консоль при первой и повторной загрузке; иначе передать проверку пользователю;
- при наличии автоматизации браузерных кликов и ввода выполнить сценарии с пустым именем, именем из пробелов, корректным именем и повторной загрузкой; иначе передать их пользователю;
- при наличии доступа к хранилищу страницы проверить содержимое `levelUpState` после сохранения имени;
- при наличии доступа к хранилищу страницы подставить невалидную JSON-строку и наблюдаемо проверить безопасный возврат к стартовой форме;
- после проверок `localStorage` восстановить чистое согласованное состояние и зафиксировать фактический результат восстановления;
- проверить отсутствие запрещённых зависимостей и файлов;
- выполнить `git diff --check`.

### Ручные проверки пользователя

- оценить понятность стартовой формы и профиля;
- проверить читаемость базовых цветов и текста;
- открыть страницу на компьютере и телефоне и убедиться, что каркас не выходит за экран.

### Фактически реализовано

- стартовая форма имени;
- запрет пустого имени и имени из пробелов;
- сохранение корректного имени;
- профиль уровня 1 с 0 XP;
- восстановление состояния после перезагрузки;
- безопасная обработка невалидного JSON;
- единственный ключ `localStorage` — `levelUpState`;
- начальное состояние `version: 1` с `profile`, `additionalSubjects` и `tasks`;
- адаптивный каркас;
- согласованная палитра:
  - `#F3E9D7`;
  - `#D6BFA6`;
  - `#B08968`;
  - `#7A553A`;
  - `#3B2A22`;
- новая компоновка блоков:
  - «Новая задача» и «Активные задачи» в одном ряду;
  - «Календарь» на всю ширину;
  - «Фильтры» и «Выполненные задачи» в одном ряду;
- кнопка настроек в верхней панели;
- панель «Учебные предметы», открывающаяся поверх страницы;
- закрытие панели повторным нажатием на кнопку-шестерёнку;
- закрытие панели клавишей Escape;
- отсутствие сдвига основного интерфейса при открытии панели.

### Фактические результаты проверок

- пустое имя не принято;
- имя из пробелов не принято;
- имя «Анастасия» сохранено;
- после перезагрузки восстановлены имя, уровень 1 и 0 XP;
- невалидный JSON привёл к безопасному возврату на стартовый экран;
- рабочее состояние после теста восстановлено;
- при открытии через `http://localhost:8000` ошибок `script.js` в Console не обнаружено;
- обнаружен только несущественный ответ `404` для `favicon.ico`;
- Node.js в среде проверки отсутствует, поэтому `node --check` не выполнялся;
- на iPhone 14 Pro Max блоки отображаются в одну колонку;
- панель настроек не выходит за границы мобильного экрана;
- на iPad и компьютере компоновка отображается корректно;
- горизонтальная прокрутка не обнаружена;
- пользователь подтвердил оформление и расположение элементов.

### Итог ручной проверки

Обязательные ручные проверки подтверждены пользователем. Шаг 1 завершён; Git-коммит не создан и ожидает отдельного подтверждения.

### Критерии завершения

- основной интерфейс недоступен без сохранённого имени;
- пустое имя не принимается, корректное сохраняется;
- профиль показывает уровень 1 и 0 XP;
- корректное состояние восстанавливается после перезагрузки;
- повреждённое состояние не вызывает падение;
- страница работает без внешних библиотек и без ошибок консоли.

### Точка сохранения Git

После успешных проверок создать отдельный commit: `Создать каркас приложения и профиль пользователя`.

---

## Шаг 2. Предметы, создание задач и валидация

**Статус:** Завершён

### Цель шага

Получить законченное создание активных задач и управление дополнительными предметами с сохранением данных.

### Файлы

- изменить `index.html`;
- изменить `style.css`;
- изменить `script.js`.

### Что реализовать

- шесть направлений: Школа, ЕГЭ, Олимпиада, Курс, Поступление и Личные дела;
- предметы по умолчанию: Русский язык и Математика;
- добавление непустого уникального предмета без дубликатов с учётом регистра;
- форму задачи с названием, направлением, предметом, сложностью и дедлайном;
- динамическую обязательность предмета по выбранному направлению;
- сложности `easy`, `medium`, `hard` и предварительное отображение 5, 20, 50 XP;
- разрешение сегодняшнего, будущего и прошедшего дедлайна;
- полную валидацию с текстовыми ошибками рядом с полями без потери введённых значений;
- создание объекта задачи по структуре из раздела 9 `tz.md`, включая уникальный `id`, оба дедлайна, нулевой счётчик переносов, `status`, `xpAwarded`, `xpReward` и `createdAt`;
- добавление новой задачи в начало `tasks`;
- сохранение предметов и задач в `levelUpState`.

### Результат шага

Пользователь может добавлять предметы и создавать корректные активные задачи всех разрешённых типов. Данные не исчезают после перезагрузки.

### Автоматические проверки Codex

Все проверки ниже выполняются автоматически только при наличии подходящего доступного инструмента. Для каждой выполненной проверки в журнал записываются инструмент или команда и фактический результат. Проверки, которые нельзя выполнить инструментально, переносятся в ручные проверки пользователя и не считаются пройденными автоматически.

- проверить синтаксис `script.js`; консоль браузера проверять только при наличии доступа к ней, иначе передать эту проверку пользователю;
- при наличии автоматизации браузерных действий проверить все шесть направлений и правила обязательности предмета;
- при наличии автоматизации ввода проверить пустой предмет, дубликат и дубликат в другом регистре;
- при наличии автоматизации ввода проверить пустое название, отсутствующую сложность, отсутствующий или некорректный дедлайн;
- при наличии автоматизации браузерных действий проверить создание учебной задачи, личной задачи без предмета и уже просроченной задачи;
- при наличии доступа к состоянию страницы проверить соответствие созданного объекта структуре и инвариантам раздела 9 `tz.md`;
- при наличии управления браузером проверить восстановление задач и дополнительных предметов после перезагрузки;
- после проверок `localStorage` восстановить чистое согласованное состояние и зафиксировать фактический результат восстановления;
- выполнить `git diff --check`.

### Ручные проверки пользователя

- оценить удобство формы на компьютере и телефоне;
- убедиться, что ошибки понятны и не обозначаются только цветом;
- проверить, что предварительный XP заметен и понятен.

### Фактически реализовано

- предметы по умолчанию «Русский язык» и «Математика»;
- добавление дополнительных предметов в панели настроек;
- удаление внешних пробелов, запрет пустых значений и дубликатов без учёта регистра;
- сохранение и восстановление `additionalSubjects` внутри `levelUpState`;
- форма создания задачи внутри блока «Новая задача»;
- все шесть направлений и динамическая обязательность предмета;
- выбор сложности `easy`, `medium`, `hard` с предварительными 5, 20 и 50 XP;
- дедлайн без запрета прошедших дат и с проверкой календарного формата `YYYY-MM-DD`;
- текстовые ошибки рядом с полями без очистки введённых значений;
- отсутствие записи в `localStorage` при невалидной форме;
- создание активной задачи по структуре раздела 9 `tz.md`;
- уникальный `id`, одинаковые `originalDeadline` и `currentDeadline`, нулевой `postponementCount`, `status: "active"`, `xpAwarded: false`, награда по сложности и ISO-время `createdAt`;
- добавление новой задачи в начало массива `tasks`;
- сохранение всего состояния под прежним единственным ключом `levelUpState`;
- успешная форма очищается, но карточки задач пока не создаются согласно границам шага 2.

### Фактические результаты проверок

- инструмент: bundled Node.js, команда `node --check script.js`; результат: код завершения `0`, синтаксических ошибок нет;
- инструмент: Python `html.parser`; результат: обязательные элементы формы и панели найдены, дублирующихся `id` нет, присутствуют все пять полей задачи;
- инструмент: `rg`; результат: внешние зависимости отсутствуют, обращения к `localStorage` используют константу единственного ключа `levelUpState`;
- инструмент: исполняемый Node.js DOM/`localStorage`-макет; результат: `PASS`, 24 проверки — базовые и дополнительные предметы, запрет пустого значения и дубликатов, сохранение предмета, обязательный и необязательный предмет, прошедший дедлайн, структура задачи, 0 XP профиля, добавление двух задач в начало массива и восстановление после повторной загрузки;
- инструмент: дополнительный Node.js DOM/`localStorage`-макет; результат: `PASS` — проверены шесть направлений, предварительные 5/20/50 XP, ошибки пустой формы, некорректная календарная дата, сохранение введённых значений и `0` записей в хранилище при ошибках;
- тестовое состояние `localStorage` после обоих наборов сценариев восстановлено до чистого согласованного состояния;
- первая попытка DOM-теста остановилась из-за неполного поведения тестового `DocumentFragment`; макет исправлен, тот же набор сценариев повторён и завершился `PASS`;
- пакет `jsdom` в доступной среде отсутствует, поэтому он не использовался;
- проверка `git diff --check` завершилась с кодом `0`;
- структура CSS проверена: 94 открывающие и 94 закрывающие фигурные скобки, два адаптивных медиаправила сохранены;
- реальный браузер, его Console, клики и визуальное отображение в этой автоматической проверке не запускались и не считаются проверенными.

### Фактические результаты ручной проверки

**Предметы:**

- по умолчанию доступны «Русский язык» и «Математика»;
- пустой предмет не добавляется;
- успешно добавлены «История» и «Физика»;
- пробелы по краям названия удаляются;
- дубликаты блокируются без учёта регистра;
- дополнительные предметы сохраняются после перезагрузки.

**Направления:**

- для «Школа», «ЕГЭ», «Олимпиада» и «Курс» предмет обязателен;
- для «Поступление» и «Личные дела» предмет необязателен;
- для необязательного предмета доступно значение «Без предмета».

**Создание задач:**

- корректно созданы задачи с наградами 5, 20 и 50 XP;
- разрешён прошедший дедлайн;
- задача успешно создана в мобильном режиме;
- новые задачи добавляются в начало массива `tasks`;
- общий XP после создания задач остаётся равен 0;
- после перезагрузки задачи сохраняются.

**Структура объекта задачи:**

- присутствуют уникальный `id`, `title`, `direction`, `subject` или `null`, `difficulty` и `xpReward`;
- установлены `status: "active"`, `postponementCount: 0` и `xpAwarded: false`;
- `originalDeadline` и `currentDeadline` совпадают;
- `createdAt` записан в ISO-формате.

**Валидация:**

- проверены пустое название, отсутствующая сложность, дедлайн и обязательный предмет;
- ошибки отображаются текстом рядом с соответствующими полями;
- при ошибке введённые корректные значения сохраняются;
- невалидные отправки не изменяют `localStorage`.

**Интерфейс:**

- ошибок `script.js` в Console не обнаружено;
- форма работает на компьютере и iPhone 14 Pro Max;
- горизонтальная прокрутка не обнаружена;
- текст отключённого поля «Предмет» — «Сначала направление» — отображается полностью и не перекрывает стрелку;
- пользователь подтвердил внешний вид и удобство формы.

### Критерии завершения

- доступны все направления, предметы и сложности;
- обязательность предмета работает по правилам направления;
- невалидная форма не изменяет постоянные данные;
- корректная задача сохраняется и появляется в состоянии;
- дополнительные предметы и задачи восстанавливаются после перезагрузки.

### Точка сохранения Git

После успешных проверок создать отдельный commit: `Добавить предметы и создание задач с валидацией`.

---

## Шаг 3. Списки, карточки, сроки и фильтрация

**Статус:** Завершён

### Цель шага

Сделать созданные задачи полноценно обозримыми: вывести карточки, рассчитать календарные статусы и добавить совместную фильтрацию.

### Файлы

- изменить `index.html`;
- изменить `style.css`;
- изменить `script.js`.

### Что реализовать

- отдельные контейнеры активных и выполненных задач;
- блок «Активные задачи» на всю ширину с кнопками «+ Новая задача» и «Фильтры задач» в заголовке;
- компактную квадратную кнопку рядом с заголовком для сворачивания и разворачивания только области карточек активных задач;
- раскрывающуюся форму создания и компактную раскрывающуюся панель фильтров с закрытием повторным нажатием и Escape и синхронизацией `aria-expanded`;
- карточку со всеми обязательными данными, бейджами статуса и предусмотренными для текущего статуса действиями без отдельной строки «Статус задачи»;
- постоянное отображение предмета с текстом `Без предмета` при `subject === null`;
- фиксированную сетку карточки из двух колонок на компьютере и согласованный порядок данных в одну колонку на телефоне;
- светлый фон `#F3E9D7` для активной карточки и более тёмный фон `#D6BFA6` для просроченной без добавления новых цветов;
- дедлайн в формате `ДД.ММ.ГГГГ` без сдвига часового пояса;
- безопасный разбор `YYYY-MM-DD` по компонентам без `new Date(currentDeadline)`;
- вычисление календарной разницы через независимые от часового пояса индексы дней;
- статусы `Сегодня`, будущий срок и `Просрочено`, включая количество календарных дней;
- текстовое и визуальное выделение строки срока и просрочки;
- фильтры статуса, направления и предмета, работающие совместно по правилу AND;
- значения предмета `Все предметы`, `Без предмета`, два базовых и все дополнительные предметы пользователя;
- компактную квадратную кнопку `×` в правом верхнем углу панели для сброса всех трёх фильтров без закрытия панели;
- предусмотренные сообщения для пустых списков и пустого результата фильтрации;
- перенос длинного текста без горизонтальной прокрутки.

### Результат шага

Активные задачи отображаются в единообразных карточках, корректно показывают срок и просрочку и фильтруются по статусу, направлению и предмету. Форма создания и фильтры раскрываются внутри блока активных задач и не занимают постоянные отдельные карточки. Фильтры меняют только представление, а не данные.

### Автоматические проверки Codex

Все проверки ниже выполняются автоматически только при наличии подходящего доступного инструмента. Для каждой выполненной проверки в журнал записываются инструмент или команда и фактический результат. Проверки, которые нельзя выполнить инструментально, переносятся в ручные проверки пользователя и не считаются пройденными автоматически.

- проверить синтаксис; консоль браузера проверять только при наличии доступа к ней, иначе передать эту проверку пользователю;
- при наличии инструмента выполнения JavaScript или управления браузером проверить даты вчера, сегодня и завтра, включая границу месяца;
- при наличии инструмента выполнения JavaScript или управления браузером проверить отображение `2026-07-15` как `15.07.2026` без сдвига;
- при наличии управления браузером проверить будущую, сегодняшнюю и просроченную активную задачу;
- при наличии автоматизации кликов проверить открытие и закрытие формы и панели фильтров повторным нажатием и Escape, а также синхронизацию `aria-expanded`;
- при наличии автоматизации кликов проверить сворачивание и разворачивание активного списка, символы `−` и `+`, значения `aria-expanded` и смену `aria-label`;
- при наличии доступа к состоянию проверить, что сворачивание не меняет `tasks` и не записывает `localStorage`;
- при наличии автоматизации создания задачи проверить автоматическое раскрытие списка и видимость новой первой карточки;
- при наличии управления браузером проверить, что форма и фильтры продолжают работать при свёрнутом списке, а кнопка скрыта при отсутствии подходящих активных задач;
- при наличии автоматизации кликов проверить все значения трёх фильтров и их совместную работу по AND;
- при наличии автоматизации кликов проверить `disabled` кнопки `×` при значениях по умолчанию, её включение после изменения одного и всех трёх фильтров и сброс к `Все`, `Все направления`, `Все предметы`;
- при наличии доступа к состоянию проверить, что сброс немедленно возвращает полный список, оставляет панель открытой, не меняет состояние `+` / `−` активного списка и при появлении активных задач снова показывает кнопку сворачивания;
- при наличии доступа к DOM проверить `type="button"`, символ `×`, `aria-label="Сбросить фильтры"`, `title="Сбросить фильтры"` и обновление `disabled` после изменения, сброса, заполнения дополнительных предметов и перерисовки;
- при наличии доступа к состоянию и хранилищам проверить, что сброс не изменяет `tasks`, `localStorage` и `sessionStorage`;
- при наличии доступа к состоянию проверить `Без предмета`, базовые и добавленные пользователем предметы в фильтре;
- при наличии доступа к хранилищу страницы проверить, что фильтрация не изменяет `levelUpState`;
- при наличии управления браузером проверить сообщения пустых состояний;
- после проверок `localStorage` восстановить чистое согласованное состояние и зафиксировать фактический результат восстановления;
- выполнить `git diff --check`.

### Ручные проверки пользователя

- оценить фиксированную структуру карточек, перенос длинного текста и визуальную заметность строки срока и просрочки;
- убедиться, что статус понятен без опоры только на цвет;
- проверить открытие и закрытие формы создания и панели фильтров, их `aria-expanded` и удобство на компьютере и телефоне;
- проверить три фильтра, включая `Без предмета` и дополнительный предмет.
- проверить кнопку `×`: отключённое состояние по умолчанию, включение после изменения одного и всех трёх фильтров, полный сброс и немедленное возвращение списка без закрытия панели;
- проверить, что сброс не меняет состояние `+` / `−`, а при повторном появлении активных задач кнопка сворачивания отображается;
- проверить `aria-label`, `title`, компактное расположение в правом верхнем углу на компьютере и телефоне и отсутствие горизонтальной прокрутки;
- проверить сворачивание активного списка, доступность формы и фильтров при свёрнутых карточках и расположение компактной кнопки рядом с заголовком на компьютере и телефоне.

### Реализованные согласованные изменения интерфейса после первой реализации

- поменять местами существующие фоны карточек: активная — `#F3E9D7`, просроченная — `#D6BFA6`; сохранить бейдж `Просрочено`, заметную границу и текст количества дней просрочки;
- убрать отдельную строку статуса задачи, оставив бейджи `Активная`, `Выполнена` и дополнительный `Просрочено`;
- всегда показывать предмет и выводить `Без предмета` при `subject === null`;
- на компьютере после строки названия и бейджей использовать пары `Направление | Предмет`, `Дедлайн | Срок`, `Сложность | Награда`; на телефоне выводить эти данные в том же порядке в одну колонку;
- выделить значение срока начертанием и существующими акцентами: будущий срок — красно-коричневый акцент, сегодня и просрочка — цвет ошибки, выполненная задача — нейтральное `Выполнено`;
- убрать отдельные постоянные карточки создания и фильтров, перенести их раскрывающиеся панели под заголовок блока «Активные задачи»;
- после успешного создания очищать и закрывать форму, показывать `Задача сохранена` и сразу выводить новую задачу первой; при ошибке оставлять форму открытой и сохранять значения;
- добавить фильтр предмета со значениями `Все предметы`, `Без предмета`, базовыми и дополнительными предметами;
- применять три фильтра совместно по правилу статус AND направление AND предмет без изменения `tasks` и `localStorage`;
- сохранить основной порядок: профиль, активные задачи на всю ширину, календарь, выполненные задачи.

### Реализованное сворачивание активного списка

- рядом с заголовком «Активные задачи» должна располагаться компактная квадратная кнопка, отдельная от крупных кнопок «+ Новая задача» и «Фильтры задач»;
- развёрнутое состояние обозначается символом `−`, `aria-expanded="true"` и `aria-label="Свернуть активные задачи"`;
- свёрнутое состояние обозначается символом `+`, `aria-expanded="false"` и `aria-label="Развернуть активные задачи"`;
- скрывается только область карточек активных задач; заголовок, основные кнопки, форма и фильтры остаются видимыми и рабочими;
- сворачивание не изменяет `tasks` и `localStorage` и не сохраняется между перезагрузками;
- после перезагрузки список развёрнут по умолчанию;
- после успешного создания задачи список автоматически разворачивается и показывает новую карточку;
- при отсутствии подходящих активных задач кнопка не отображается;
- на телефоне кнопка остаётся рядом с заголовком и не переносится к крупным управляющим кнопкам.

Изменение реализовано в `index.html`, `style.css` и `script.js`. Состояние хранится только в переменной текущего запуска и не добавляется в постоянную структуру данных.

### Согласованное изменение: сброс фильтров

- в правом верхнем углу раскрытой панели фильтров добавить визуально отделённую от трёх `select` компактную квадратную кнопку с символом `×`;
- использовать `type="button"`, `aria-label="Сбросить фильтры"` и `title="Сбросить фильтры"`;
- при значениях `Все`, `Все направления` и `Все предметы` держать кнопку отключённой и визуально обозначать `disabled` только существующей палитрой;
- включать кнопку после изменения одного или нескольких фильтров; пересчитывать состояние после изменения, сброса, заполнения дополнительных предметов и перерисовки;
- по нажатию сбрасывать все три значения, немедленно обновлять оба списка и оставлять панель открытой;
- не менять пользовательское состояние сворачивания активного списка, `tasks`, `localStorage` и `sessionStorage`;
- если после сброса появились активные задачи, снова показывать кнопку `+` / `−` рядом с заголовком;
- на компьютере и телефоне сохранять компактный квадратный размер, безопасное расположение без перекрытий и горизонтальной прокрутки.

Изменение реализовано в `index.html`, `style.css` и `script.js`: кнопка находится внутри панели, сбрасывает только три фильтра и не добавляет состояние в структуру данных или хранилища.

### Фактически реализовано до согласованной корректировки интерфейса

Перечень ниже сохранён как историческая запись состояния первой реализации шага 3 до корректировки интерфейса. Актуальная реализация и повторные проверки зафиксированы в следующих подразделах.

- активные и выполненные задачи выводятся в существующих разделах в порядке массива `tasks`;
- после успешного создания новой задачи активный список перерисовывается без перезагрузки;
- добавлены карточки с названием, направлением, предметом при наличии, сложностью, наградой XP, текущим дедлайном, состоянием срока, статусом задачи и счётчиком переносов при значении больше нуля;
- работающие кнопки редактирования, выполнения и удаления не добавлялись согласно границам шага 3;
- дата `YYYY-MM-DD` разбирается по компонентам и отображается как `ДД.ММ.ГГГГ` без `new Date(currentDeadline)`;
- разница календарных дней рассчитывается через независимые UTC-индексы дня на основе локальных компонентов сегодняшней даты;
- реализованы состояния `Сегодня`, будущий срок с количеством дней, `Просрочено` с количеством дней просрочки и `Выполнено` без просрочки;
- просрочка обозначается текстом `Просрочено`, отдельным бейджем, рамкой и акцентом карточки;
- добавлены фильтры по четырём статусам и семи значениям направления, работающие совместно по правилу AND;
- фильтры изменяют только представление и сразу перерисовывают списки;
- добавлены отдельные сообщения для действительно пустого списка и отсутствия результатов по выбранным фильтрам;
- карточки, детали и фильтры адаптированы к одной колонке на телефоне, длинные значения переносятся;
- согласованный текст отключённого поля «Предмет» — «Сначала направление» — сохранён.

### Фактические результаты проверок первой реализации

- инструмент: bundled Node.js, команда `node --check script.js`; результат: код завершения `0`, синтаксических ошибок не обнаружено;
- инструмент: Python `html.parser`; результат: обязательные контейнеры списков и оба фильтра найдены, дублирующихся `id` нет, фильтр статуса содержит 4 требуемых значения, фильтр направления — 7 требуемых значений;
- инструмент: изолированный Node.js VM с DOM-макетом; результат: `PASS`, 20 проверок — формат `2026-07-15` преобразован в `15.07.2026`, вчера/сегодня/завтра корректны на границе января и февраля, выполненная задача не считается просроченной;
- тем же VM-тестом выполнены все 28 сочетаний четырёх статусов и семи направлений; подтверждены фильтры `Все`, `Активные`, `Выполненные`, `Просроченные`, совместная работа по AND и исключение выполненной задачи из просроченных;
- тем же VM-тестом подтверждены сохранение порядка массива, сообщения пустых списков и отдельное сообщение пустого результата фильтрации;
- снимок массива `tasks` до и после всех сочетаний фильтров совпал; число записей в макет `localStorage` равно `0`;
- тесты выполнялись только в памяти отдельного процесса и не обращались к реальному `levelUpState`; после завершения процесса изолированное тестовое состояние уничтожено, состояние приложения не изменялось;
- структура CSS проверена подсчётом фигурных скобок: 119 открывающих и 119 закрывающих;
- команда `git diff --check` завершилась с кодом `0`;
- первая попытка VM-проверки русского текста остановилась на сравнении строки, переданной через PowerShell; фактическое значение функции было отдельно выведено и оказалось корректным, после чего тот же сценарий повторён с Unicode-последовательностями и завершён `PASS`;
- реальный браузер, его Console, клики, размеры экрана и визуальное отображение автоматически не проверялись.

### Фактически реализовано после согласованной корректировки

- отдельные постоянные карточки «Новая задача» и «Обзор / Фильтры» удалены из основного потока;
- блок «Активные задачи» занимает всю ширину и содержит в заголовке кнопки «+ Новая задача» и «Фильтры задач»;
- непосредственно рядом с заголовком добавлена компактная квадратная кнопка сворачивания активных карточек с символами `−` и `+`, изменяемыми `aria-expanded` и `aria-label`;
- кнопка управляет только контейнером активного списка, не скрывая заголовок, форму, фильтры, календарь или выполненные задачи;
- пользовательское состояние сворачивания сохраняется при переключении фильтров, кнопка скрывается при пустом активном результате и появляется снова вместе с задачами;
- состояние не записывается в хранилища, при новом запуске список развёрнут, а успешное создание принудительно разворачивает его;
- форма создания и компактная панель фильтров независимо раскрываются внутри блока, закрываются повторным нажатием и Escape и синхронизируют `aria-expanded`;
- обе панели участвуют в обычном потоке страницы и не перекрывают карточки; они могут быть открыты одновременно;
- форма на компьютере использует пары `Направление | Предмет`, `Сложность | Дедлайн`, `Награда | Добавить задачу`, а название занимает всю ширину; на телефоне элементы идут в одну колонку;
- при ошибке форма остаётся открытой и сохраняет значения; после успеха очищается и закрывается, показывает `Задача сохранена`, а новая задача появляется первой;
- добавлен третий фильтр по предмету: `Все предметы`, `Без предмета`, базовые и дополнительные предметы пользователя;
- значения предметов фильтра кодируются отдельно от служебных значений, поэтому пользовательский предмет не конфликтует с `Все предметы` или `Без предмета`;
- три фильтра работают по правилу статус AND направление AND предмет, сразу обновляют списки и не изменяют `tasks` или `localStorage`;
- при фильтре `Выполненные` скрывается только содержимое активного списка, поэтому кнопки и панель фильтров остаются доступны для смены значения;
- карточка содержит фиксированные шесть полей в порядке `Направление`, `Предмет`, `Дедлайн`, `Срок`, `Сложность`, `Награда`; при `subject === null` выводится `Без предмета`;
- отдельная строка статуса удалена; статус передаётся бейджами `Активная`, `Выполнена` и дополнительным `Просрочено`;
- активная карточка использует `#F3E9D7`, просроченная — `#D6BFA6`; просрочка сохраняет заметную границу, бейдж и текст количества дней;
- выполненная карточка отличается бейджем и цветом левой границы, показывает нейтральный срок `Выполнено` и не считается просроченной;
- значение срока выделено жирным: будущий срок использует существующий коричневый акцент, сегодня и просрочка — существующий цвет ошибки;
- после активных задач сохранён порядок блоков: календарь, затем выполненные задачи;
- профиль, верхняя панель, структура данных, валидация, XP-награды и ключ `levelUpState` не изменялись; функции следующих шагов не добавлялись.

### Фактические результаты повторных инструментальных проверок

- инструмент: bundled Node.js, команда `node --check script.js`; результат: код завершения `0`, синтаксических ошибок нет;
- инструмент: Python `html.parser`; результат: новые кнопки, панели, третий фильтр и контейнеры списков найдены, дублирующихся `id` нет;
- проверка HTML и CSS на внешние адреса и `@import`; результат: внешние библиотеки и зависимости не обнаружены;
- структура CSS проверена подсчётом фигурных скобок: 139 открывающих и 139 закрывающих;
- набор шестнадцатеричных цветов CSS проверен Python: используются только существующие `#F3E9D7`, `#D6BFA6`, `#B08968`, `#7A553A`, `#3B2A22` и `#8F312B`, новых цветов нет;
- изолированный Node.js VM-тест: `PASS`, 11 проверок и 112 сочетаний четырёх статусов, семи направлений с `Все направления` и четырёх классов значений предмета; подтверждены AND, `Без предмета`, базовый и дополнительный предмет, пустое и отфильтрованное состояния;
- в том же тесте снимок состояния до и после фильтрации совпал, число записей в макет `localStorage` равно `0`, тестовое состояние восстановлено из независимой копии;
- отдельный Node.js VM-тест панелей: `PASS`, 12 проверок — базовые и дополнительные предметы появились в фильтре, обе панели открываются и закрываются повторным нажатием, Escape закрывает обе, `aria-expanded` синхронизируется, записей в хранилище нет;
- Node.js VM-тест успешного создания: `PASS`, 7 проверок — выполнена одна запись под ключом `levelUpState`, существующая задача сохранена, новая добавлена первой, XP не изменён, форма очищена и закрыта, сообщение `Задача сохранена` видимо;
- Node.js VM-тест невалидной формы: `PASS` — форма осталась открытой, корректные значения сохранились, текстовая ошибка видима, записей в хранилище `0`;
- Node.js VM-тест карточек: `PASS` — шесть полей имеют фиксированный порядок, `Без предмета` видно, отдельной строки статуса нет, просрочка имеет два бейджа, выполненный срок нейтрален;
- отдельный VM-тест подтвердил, что фильтр `Выполненные` скрывает только активный список, а панель фильтров остаётся доступной;
- команда `git diff --check` завершилась с кодом `0`;
- первые попытки отдельных VM-проверок пустого состояния и кириллических подписей остановились из-за общей ссылки на тестовый объект и кодировки PowerShell; стенд исправлен с независимой копией и ASCII/Unicode-последовательностями, полные сценарии повторены и завершены `PASS`;
- реальный браузер, его Console, клики и визуальное отображение автоматически не проверялись; реальные данные браузера `levelUpState` инструментальные тесты не изменяли.

### Фактические результаты проверки сворачивания активного списка

- инструмент: bundled Node.js, команда `node --check script.js`; результат: код завершения `0`, синтаксических ошибок нет;
- инструмент: Python `html.parser`; результат: кнопка найдена, имеет `type="button"`, `aria-controls="active-tasks-content"`, начальные `aria-expanded="true"` и `aria-label="Свернуть активные задачи"`; дублирующихся `id` нет;
- структура CSS проверена подсчётом фигурных скобок: 145 открывающих и 145 закрывающих;
- изолированный Node.js VM-тест: `PASS`, 10 проверок — начальное развёрнутое состояние, сворачивание только активного списка, повторное разворачивание, сохранение порядка карточек, доступность открытых формы и фильтров, скрытие кнопки при пустом результате и повторное появление без сброса пользовательского состояния;
- тем же VM-тестом подтверждены неизменность `tasks`, `0` записей в макет `localStorage` и развёрнутое состояние в новом экземпляре среды, имитирующем перезагрузку;
- отдельный Node.js VM-тест успешного создания: `PASS` — выполнена одна штатная запись состояния задачи, новая задача добавлена первой, ранее свёрнутый список автоматически развёрнут и новая карточка видна первой;
- поиск по `script.js` подтвердил отсутствие использования `sessionStorage`;
- первая попытка VM-теста остановилась из-за искажения кириллического `aria-label` и символа `−` при передаче через PowerShell; тот же сценарий повторён с Unicode-последовательностями и завершён `PASS`;
- реальный браузер, Console, клики, hover, focus и фактические размеры экрана автоматически не проверялись.

### Фактические результаты проверки кнопки сброса фильтров

- инструмент: bundled Node.js, команда `node --check script.js`; результат: код завершения `0`, синтаксических ошибок нет;
- инструмент: Python `html.parser`; результат: `PASS`, подтверждены `type="button"`, начальный `disabled`, `aria-label="Сбросить фильтры"`, `title="Сбросить фильтры"` и символ Unicode `×`;
- первая попытка проверки русских атрибутов через PowerShell остановилась из-за кодировки входного текста; проверка повторена с Unicode-последовательностями и завершена `PASS`;
- изолированный Node.js VM-тест: `PASS` — при трёх значениях `all` кнопка отключена, изменение одного и всех трёх фильтров включает её, сброс возвращает три значения `all`, вызывает одну перерисовку и снова отключает кнопку;
- тем же VM-тестом подтверждены открытое состояние панели, сохранение свёрнутого состояния активного списка, неизменность `tasks`, `0` записей в макеты `localStorage` и `sessionStorage` и отсутствие обращений к хранилищам из функции сброса;
- реальный встроенный браузер: при открытии панели кнопка отображает `×` и отключена; изменение одного фильтра включает её, изменение всех трёх оставляет активной, нажатие возвращает `Все`, `Все направления` и `Все предметы` и оставляет панель открытой;
- реальный встроенный браузер: после фильтра без совпадений кнопка сворачивания активного списка скрылась, после сброса появилась снова; ранее выбранное свёрнутое состояние сохранилось, а карточка вернулась в DOM немедленно;
- реальный встроенный браузер: после добавления тестового дополнительного предмета он появился в фильтре, значение `Все предметы` сохранилось, кнопка сброса осталась корректно отключённой;
- реальный встроенный браузер на размерах `1280 × 900` и `430 × 932`: кнопка имеет размер `40 × 40` пикселей, находится справа сверху с внутренними отступами около 18 пикселей, не перекрывает поля, панель не выходит за экран и горизонтальная прокрутка отсутствует;
- Console встроенного браузера: ошибок не обнаружено; тест выполнялся в отдельной браузерной вкладке, которая после проверки закрыта;
- CSS проверен по списку шестнадцатеричных значений: новых цветов не добавлено;
- команда `git diff --check -- index.html style.css script.js plan.md` завершилась с кодом `0`.

### Итоги финальной ручной проверки

#### Сворачивание активных задач

- компактная кнопка расположена непосредственно рядом с заголовком «Активные задачи», отдельно от кнопок «+ Новая задача» и «Фильтры задач»;
- в развёрнутом состоянии отображаются символ `−`, `aria-expanded="true"`, `aria-label="Свернуть активные задачи"` и `aria-controls="active-tasks-content"`;
- в свёрнутом состоянии отображаются символ `+`, `aria-expanded="false"`, `aria-label="Развернуть активные задачи"` и `aria-controls="active-tasks-content"`;
- скрываются только карточки активных задач и их пустое состояние; заголовок и основные управляющие кнопки остаются видимыми;
- форма создания и панель фильтров продолжают работать при свёрнутом списке;
- календарь и выполненные задачи не затрагиваются;
- после перезагрузки список развёрнут, а после успешного создания новой задачи разворачивается автоматически;
- временная тестовая задача после проверки удалена восстановлением состояния;
- при отсутствии задач по текущим фильтрам кнопка скрывается, при появлении подходящих задач возвращается;
- состояние сворачивания сохраняется при переключении и сбросе фильтров;
- сворачивание не изменяет `tasks`, `localStorage` или `sessionStorage`.

#### Кнопка сброса фильтров

- компактная кнопка `×` расположена в правом верхнем углу панели, не перекрывает подписи и поля;
- при значениях по умолчанию кнопка имеет `disabled=true`, изменение любого фильтра активирует её;
- подтверждены символ `×`, `aria-label="Сбросить фильтры"` и `title="Сбросить фильтры"`;
- нажатие устанавливает статус `Все`, направление `Все направления` и предмет `Все предметы`;
- полный список обновляется немедленно, панель остаётся открытой, состояние сворачивания не изменяется;
- после сброса кнопка снова становится отключённой;
- количество задач в `localStorage` до и после сброса равно 6;
- сброс не изменяет `tasks`, `localStorage` или `sessionStorage`.

#### Компьютер и телефон

- кнопка `+` / `−` остаётся рядом с заголовком на компьютере и iPhone 14 Pro Max, а основные управляющие кнопки располагаются отдельно;
- форма и фильтры работают при свёрнутом списке;
- кнопка `×` остаётся компактной на телефоне;
- элементы не перекрываются, горизонтальная прокрутка отсутствует;
- hover- и focus-состояния отображаются визуально;
- ошибок `script.js` в реальной Console не обнаружено.

#### Итоговое состояние

- сохранено 6 активных задач, выполненных задач нет;
- общий XP равен 0;
- временные тестовые изменения удалены;
- пользователь подтвердил работу и оформление интерфейса шага 3.

### Критерии завершения

- карточка содержит все данные, требуемые разделом 17 `tz.md`;
- карточка соблюдает фиксированную структуру, всегда показывает предмет и передаёт статус только бейджами;
- форма создания и панель трёх фильтров корректно раскрываются, закрываются и синхронизируют `aria-expanded`;
- активный список сворачивается независимо от формы и фильтров, не изменяет состояние, корректно обновляет символ, `aria-expanded` и `aria-label`;
- после создания задачи и после перезагрузки активный список развёрнут, а при отсутствии подходящих задач кнопка скрыта;
- даты и количество дней корректны для локальной календарной даты;
- выполненная задача не считается просроченной;
- три фильтра содержат все значения и корректно комбинируются по правилу AND без изменения состояния;
- кнопка `×` корректно сбрасывает три фильтра, имеет требуемые доступные подписи и `disabled`-состояние, не закрывает панель и не изменяет данные или состояние сворачивания;
- длинные значения не ломают компоновку.

### Точка сохранения Git

После успешных проверок создать отдельный commit: `Добавить карточки задач, сроки и фильтры`.

---

## Шаг 4. Редактирование и учёт переносов дедлайна

**Статус:** Не начат

### Цель шага

Получить безопасное редактирование активной задачи с точным соблюдением правил дедлайна и счётчика переносов.

### Файлы

- изменить `index.html`;
- изменить `style.css`;
- изменить `script.js`.

### Что реализовать

- открытие формы редактирования только для активной задачи;
- заполнение формы текущими значениями;
- изменение названия, направления, предмета, сложности и текущего дедлайна;
- ту же валидацию, что при создании;
- применение изменений только после успешного сохранения;
- отмену без изменения объекта и `localStorage`;
- сравнение старого сохранённого и нового дедлайна при сохранении;
- увеличение `postponementCount` ровно на 1 только при более позднем дедлайне;
- отсутствие изменения счётчика при той же или более ранней дате, изменении других полей, отмене и ошибке валидации;
- неизменность `originalDeadline`;
- подпись `Переносов дедлайна: N` только при положительном счётчике;
- сохранение редактирования и счётчика после перезагрузки.

### Результат шага

Пользователь может изменить активную задачу или отменить редактирование. Счётчик переносов изменяется только по согласованному правилу и сохраняется.

### Автоматические проверки Codex

Все проверки ниже выполняются автоматически только при наличии подходящего доступного инструмента. Для каждой выполненной проверки в журнал записываются инструмент или команда и фактический результат. Проверки, которые нельзя выполнить инструментально, переносятся в ручные проверки пользователя и не считаются пройденными автоматически.

- проверить синтаксис; консоль браузера проверять только при наличии доступа к ней, иначе передать эту проверку пользователю;
- при наличии автоматизации браузерных действий проверить изменение каждого разрешённого поля;
- при наличии автоматизации кликов и доступа к состоянию проверить отмену и неуспешную валидацию без изменения состояния;
- при наличии автоматизации ввода проверить более позднюю, ту же и более раннюю дату;
- при наличии автоматизации ввода проверить несколько изменений даты до одного сохранения;
- при наличии доступа к состоянию проверить неизменность `originalDeadline`;
- при наличии доступа к состоянию проверить изменение `xpReward` при смене сложности активной задачи;
- при наличии управления браузером проверить подпись счётчика и восстановление после перезагрузки;
- после проверок `localStorage` восстановить чистое согласованное состояние и зафиксировать фактический результат восстановления;
- выполнить `git diff --check`.

### Ручные проверки пользователя

- оценить понятность открытия, сохранения и отмены редактирования;
- убедиться, что форма не создаёт визуальной путаницы с формой новой задачи;
- проверить читаемость подписи количества переносов.

### Критерии завершения

- редактируются только активные задачи;
- отмена и ошибки не изменяют данные;
- более поздний дедлайн увеличивает счётчик ровно один раз за сохранение;
- та же и более ранняя дата счётчик не увеличивают;
- `originalDeadline` остаётся первоначальным;
- результат сохраняется после перезагрузки.

### Точка сохранения Git

После успешных проверок создать отдельный commit: `Добавить редактирование задач и счётчик переносов`.

---

## Шаг 5. Выполнение, удаление, XP и уровни

**Статус:** Не начат

### Цель шага

Завершить основной жизненный цикл задачи и обеспечить единый корректный источник истины для XP, уровня и прогресса.

### Файлы

- изменить `index.html`;
- изменить `style.css`;
- изменить `script.js`.

### Что реализовать

- выполнение только активной задачи;
- одновременное изменение `status` на `completed` и `xpAwarded` на `true`;
- защиту от повторного начисления XP до и после перезагрузки;
- отдельное отображение выполненных задач без кнопок выполнения и редактирования;
- пересчёт `totalXp` как суммы `xpReward` всех сохранённых выполненных задач;
- формулы уровня, XP внутри уровня, XP до следующего уровня и процента шкалы;
- подтверждение удаления активной и выполненной задачи;
- предупреждение о вычитаемом XP при удалении выполненной задачи;
- удаление активной задачи без изменения XP;
- удаление выполненной задачи с пересчётом XP, уровня и шкалы по оставшимся выполненным задачам;
- нижнюю границу XP, равную нулю;
- сохранение операции одним обновлением состояния и восстановление после перезагрузки.

### Результат шага

Пользователь может выполнить или удалить задачу. XP начисляется один раз, всегда соответствует сохранённым выполненным задачам, а профиль корректно пересчитывается после удаления.

### Автоматические проверки Codex

Все проверки ниже выполняются автоматически только при наличии подходящего доступного инструмента. Для каждой выполненной проверки в журнал записываются инструмент или команда и фактический результат. Проверки, которые нельзя выполнить инструментально, переносятся в ручные проверки пользователя и не считаются пройденными автоматически.

- проверить синтаксис; консоль браузера проверять только при наличии доступа к ней, иначе передать эту проверку пользователю;
- при наличии автоматизации браузерных действий и доступа к состоянию проверить награды 5, 20 и 50 XP;
- при наличии автоматизации кликов и перезагрузки проверить повторное выполнение без повторного XP;
- при наличии доступа к состоянию проверить переход 80 → 100 XP, уровень 1 → 2 и прогресс 0 из 100;
- при наличии автоматизации кликов и доступа к состоянию проверить удаление активной задачи без изменения XP;
- при наличии автоматизации кликов проверить отмену удаления без изменения состояния;
- при наличии доступа к состоянию проверить удаление выполненной средней задачи при 110 XP: результат 90 XP и уровень 1;
- при наличии доступа к состоянию проверить равенство `totalXp` сумме наград оставшихся выполненных задач после каждой операции;
- при наличии доступа к состоянию проверить невозможность отрицательного XP;
- после проверок `localStorage` восстановить чистое согласованное состояние и зафиксировать фактический результат восстановления;
- выполнить `git diff --check`.

### Ручные проверки пользователя

- оценить понятность реакции на выполнение и обновления профиля;
- проверить понятность подтверждений удаления;
- убедиться, что выполненная задача явно отделена от активных.

### Критерии завершения

- XP начисляется только один раз за задачу;
- выполненные задачи нельзя редактировать или выполнить повторно;
- профиль всегда вычисляется из актуального общего XP;
- удаление активной задачи не меняет XP;
- удаление выполненной задачи вычитает ровно её награду и пересчитывает уровень;
- состояние корректно восстанавливается после перезагрузки.

### Точка сохранения Git

После успешных проверок создать отдельный commit: `Реализовать выполнение, удаление и пересчёт XP`.

---

## Шаг 6. Обзорный месячный календарь

**Статус:** Не начат

### Цель шага

Добавить месячный обзор активных задач после завершения основной логики задач и хранения.

### Файлы

- изменить `index.html`;
- изменить `style.css`;
- изменить `script.js`.

### Что реализовать

- календарь одного месяца с неделей от понедельника до воскресенья;
- заголовок месяца и года на русском языке;
- кнопки `Предыдущий месяц`, `Сегодня`, `Следующий месяц`;
- текущий месяц и сегодняшний выбранный день при запуске;
- выбор первого дня открытого месяца после перехода вперёд или назад;
- немедленное обновление мобильного списка для выбранного дня без искусственной генерации клика;
- возврат к сегодняшней дате и обновление списка по кнопке `Сегодня`;
- размещение только активных задач по `currentDeadline`;
- порядок задач согласно массиву `tasks`;
- автоматическую перерисовку после создания, редактирования, выполнения и удаления;
- независимость календаря от фильтров основных списков;
- на компьютере: до трёх однострочных названий и `+ ещё N`;
- на телефоне до 767 пикселей: количество задач в ячейке, выбор дня и полный список активных задач этого дня под сеткой;
- отсутствие создания, редактирования, переноса перетаскиванием, недельного и дневного режимов.

### Результат шага

Календарь показывает активные задачи в днях актуальных дедлайнов, работает на компьютере и телефоне и остаётся только обзорным представлением.

### Автоматические проверки Codex

Все проверки ниже выполняются автоматически только при наличии подходящего доступного инструмента. Для каждой выполненной проверки в журнал записываются инструмент или команда и фактический результат. Проверки, которые нельзя выполнить инструментально, переносятся в ручные проверки пользователя и не считаются пройденными автоматически.

- проверить синтаксис; консоль браузера проверять только при наличии доступа к ней, иначе передать эту проверку пользователю;
- при наличии инструмента выполнения JavaScript или управления браузером проверить месяцы с разным числом дней и разным смещением первого дня недели;
- при наличии инструмента выполнения JavaScript или управления браузером проверить переход декабрь → январь и январь → декабрь;
- при наличии автоматизации кликов проверить кнопки перехода и `Сегодня`;
- при наличии автоматизации браузерных действий проверить перемещение задачи после изменения дедлайна;
- при наличии автоматизации браузерных действий проверить исчезновение задачи после выполнения или удаления;
- при наличии автоматизации кликов проверить, что выполненные задачи и фильтры списков не влияют на календарь;
- при наличии управления браузером проверить 0, 1, 3 и более 3 задач в одном дне;
- при наличии управления браузером, изменения ширины экрана и автоматизации кликов проверить мобильный выбор дня и обновление списка при смене месяца; иначе передать проверку пользователю;
- при наличии управления браузером и точного изменения ширины экрана проверить горизонтальную прокрутку на ширинах 767 и 768 пикселей; иначе передать проверку пользователю;
- после проверок `localStorage` восстановить чистое согласованное состояние и зафиксировать фактический результат восстановления;
- выполнить `git diff --check`.

### Ручные проверки пользователя

- оценить читаемость календаря на компьютере;
- проверить удобство выбора дня и списка задач на телефоне;
- убедиться, что сегодняшний и выбранный день различимы не только по цвету;
- убедиться, что календарь воспринимается как обзор и не перегружает страницу.

### Критерии завершения

- календарь правильно строит текущий и соседние месяцы с понедельника;
- показывает только активные задачи по текущему дедлайну;
- реагирует на все изменения задач;
- настольное и мобильное представления соответствуют разделу 19 `tz.md`;
- календарь не изменяет данные задач и не добавляет запрещённых действий;
- на телефоне отсутствует горизонтальная прокрутка.

### Точка сохранения Git

После успешных проверок создать отдельный commit: `Добавить месячный календарь активных задач`.

---

## Шаг 7. Адаптивная полировка и финальная приёмка MVP

**Статус:** Не начат

### Цель шага

Довести уже работающий MVP до согласованного визуального, адаптивного и проверяемого состояния без добавления новых функций.

### Файлы

- изменить `index.html` при необходимости исправления структуры или доступных подписей;
- изменить `style.css`;
- изменить `script.js` только для исправления найденных несоответствий `tz.md`.

### Что реализовать

- финальную пастельную кремово-бежевую и коричневую стилизацию;
- ограниченную читаемую ширину на компьютере и одну колонку на телефоне;
- устойчивое поведение форм, карточек, кнопок, фильтров и календаря на контрольных ширинах;
- достаточно крупные элементы управления для телефона;
- перенос длинного текста и действий без горизонтальной прокрутки;
- текстовые признаки ошибок, статусов, сегодняшнего и выбранного дня в дополнение к цвету;
- только лёгкие необязательные анимации из `tz.md`;
- отключение или минимизацию анимаций при `prefers-reduced-motion: reduce`;
- исправление всех несоответствий, найденных при прохождении сценариев и критериев приёмки.

### Результат шага

Полностью готовый статический MVP из трёх кодовых файлов, прошедший все пользовательские сценарии и критерии приёмки `tz.md`.

### Автоматические проверки Codex

Все проверки ниже выполняются автоматически только при наличии подходящего доступного инструмента. Для каждой выполненной проверки в журнал записываются инструмент или команда и фактический результат. Проверки, которые нельзя выполнить инструментально, переносятся в ручные проверки пользователя и не считаются пройденными автоматически.

- проверить синтаксис всех кодовых файлов; консоль браузера проверять только при наличии доступа к ней, иначе передать эту проверку пользователю;
- при наличии управления браузером и автоматизации пользовательских действий пройти сценарии 1–13 из раздела 23 `tz.md`; каждый недоступный для автоматизации сценарий явно передать пользователю и не считать автоматически пройденным;
- проверить инструментально доступные критерии раздела 24 `tz.md`, а остальные явно передать пользователю;
- при наличии управления браузером проверить повторную загрузку после каждой операции, изменяющей состояние;
- при наличии доступа к хранилищу страницы проверить невалидный JSON и корректное восстановление валидного состояния;
- при наличии управления браузером и точного изменения ширины экрана проверить ширины 767 и 768 пикселей, а также типичный телефонный и компьютерный экран; иначе передать эти проверки пользователю;
- при наличии управления браузером и измерения размеров страницы проверить отсутствие горизонтальной прокрутки; иначе передать проверку пользователю;
- проверить отсутствие внешних библиотек, сетевых зависимостей и функций за пределами MVP;
- после проверок `localStorage` восстановить чистое согласованное состояние и зафиксировать фактический результат восстановления;
- выполнить `git diff --check` и проверить итоговый `git status`.

### Ручные проверки пользователя

- пройти основные сценарии как конечный пользователь на компьютере и телефоне;
- оценить пастельный дизайн, читаемость, понятность сообщений и визуальную иерархию;
- проверить удобство форм, карточек, фильтров и календаря;
- подтвердить соответствие итогового интерфейса ожиданиям перед публикацией.

### Критерии завершения

- все сценарии 1–13 проходят без ошибок;
- выполнены все критерии приёмки раздела 24 `tz.md`;
- данные и вычисляемые значения остаются согласованными после перезагрузки;
- интерфейс корректен на компьютере и телефоне;
- в проект не добавлены функции или технологии за пределами согласованного MVP;
- пользователь подтвердил ручную проверку.

### Точка сохранения Git

После успешных проверок создать отдельный commit: `Завершить адаптивность и проверку MVP`.

---

## Общий финальный чек-лист соответствия tz.md

### Технологии и границы

- [ ] Приложение состоит из `index.html`, `style.css` и `script.js`.
- [ ] Используются только HTML, CSS, JavaScript и `localStorage`.
- [ ] Нет React, других фреймворков, сборщиков, библиотек, сервера и базы данных.
- [ ] Не реализованы функции из раздела 25 `tz.md`.

### Первый запуск и профиль

- [ ] Без имени основной интерфейс не открывается.
- [ ] Пустое имя не принимается; корректное сохраняется и восстанавливается.
- [ ] Новый пользователь начинает с уровня 1 и 0 XP.
- [ ] Профиль показывает общий XP, уровень, прогресс и XP до следующего уровня.

### Направления, предметы и создание задач

- [ ] Доступны все шесть направлений.
- [ ] Русский язык и Математика доступны по умолчанию.
- [ ] Дополнительный предмет непустой, уникальный без учёта регистра и сохраняется.
- [ ] Обязательность предмета зависит от направления.
- [ ] Форма содержит все поля и не изменяет данные при ошибке.
- [ ] Прошедший дедлайн разрешён и сразу получает статус просрочки.
- [ ] Объекты задач соответствуют структуре и инвариантам раздела 9 `tz.md`.

### Даты, карточки и фильтры

- [ ] Дедлайн хранится как `YYYY-MM-DD` и показывается как `ДД.ММ.ГГГГ`.
- [ ] Расчёт календарных дней не зависит от времени суток и часового пояса.
- [ ] Вчера, сегодня и завтра корректно определяются на границе месяца.
- [ ] Карточки содержат все обязательные данные и разрешённые действия.
- [ ] Просрочка обозначается текстом и визуально.
- [ ] Фильтры статуса и направления работают совместно по AND и не меняют данные.

### Редактирование и переносы

- [ ] Редактируются только активные задачи.
- [ ] Изменения применяются только после успешного сохранения.
- [ ] Отмена и ошибки не изменяют состояние.
- [ ] Более поздний дедлайн увеличивает счётчик ровно на 1 за сохранение.
- [ ] Та же или более ранняя дата счётчик не увеличивает.
- [ ] `originalDeadline` не изменяется.
- [ ] При положительном счётчике показывается `Переносов дедлайна: N`.

### Выполнение, удаление, XP и уровни

- [ ] Простая задача даёт 5 XP, средняя 20 XP, сложная 50 XP.
- [ ] XP начисляется только при первом выполнении активной задачи.
- [ ] Повторное действие и перезагрузка не начисляют XP повторно.
- [ ] Выполненная задача не редактируется и не возвращается в активные.
- [ ] `totalXp` равен сумме наград сохранённых выполненных задач.
- [ ] Уровень и шкала вычисляются по формулам раздела 14 `tz.md`.
- [ ] Удаление активной задачи не меняет XP.
- [ ] Удаление выполненной задачи пересчитывает XP, уровень и прогресс.
- [ ] XP не может стать отрицательным.
- [ ] Любое удаление требует подтверждения, отмена ничего не меняет.

### localStorage

- [ ] Всё постоянное состояние хранится одним JSON под ключом `levelUpState`.
- [ ] Сохранение выполняется после каждой операции из раздела 21 `tz.md`.
- [ ] После загрузки проверяются основные поля, XP и уровень пересчитываются.
- [ ] Валидные данные восстанавливаются после перезагрузки.
- [ ] Невалидный JSON не приводит к падению и запускает безопасное начальное состояние.

### Месячный календарь

- [ ] При запуске открыт текущий месяц, выбран сегодняшний день.
- [ ] Неделя начинается с понедельника.
- [ ] Работают переходы по месяцам и кнопка `Сегодня`.
- [ ] После смены месяца выбирается первое число и обновляется мобильный список.
- [ ] Календарь показывает только активные задачи по `currentDeadline`.
- [ ] Выполнение, удаление и редактирование задачи обновляют календарь.
- [ ] Фильтры основных списков не влияют на календарь.
- [ ] На компьютере видны до трёх названий и `+ ещё N`.
- [ ] На телефоне видны количества и список задач выбранного дня.
- [ ] Календарь не создаёт, не редактирует и не переносит задачи.
- [ ] Нет недельного и дневного режимов.

### Адаптивность и оформление

- [ ] На компьютере контент ограничен по ширине и читаем.
- [ ] На телефоне блоки идут в одну колонку.
- [ ] На ширинах 767 и 768 пикселей нет горизонтальной прокрутки.
- [ ] Текст и действия не выходят за границы карточек и форм.
- [ ] Статусы, ошибки, сегодняшний и выбранный день понятны не только по цвету.
- [ ] Анимации необязательны и учитывают `prefers-reduced-motion`.

### Итоговая приёмка

- [ ] Сценарии 1–13 раздела 23 `tz.md` пройдены доступными инструментами; сценарии, которые нельзя автоматизировать в текущей среде, явно переданы пользователю и подтверждены вручную.
- [ ] Выполнены все критерии раздела 24 `tz.md`.
- [ ] Пользователь выполнил и подтвердил ручные проверки.
- [ ] Каждый завершённый шаг сохранён отдельным осмысленным Git-коммитом.
